import "package:flutter/material.dart";class DeviceInfoPage extends StatelessWidget{final String? childId;const DeviceInfoPage({super.key, this.childId});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text("Device Info")),body:Center(child:Text("Device Info: ${childId ?? "Unknown"}")));}
