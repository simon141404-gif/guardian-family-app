import "package:flutter/material.dart";class ChildDetailPage extends StatelessWidget{final String childId;const ChildDetailPage({super.key,required this.childId});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text("Child Detail")),body:Center(child:Text("Child: \$childId")));}
