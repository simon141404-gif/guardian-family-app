import "package:flutter/material.dart";class BiometricSetupPage extends StatelessWidget{const BiometricSetupPage({super.key});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text("Biometric Setup")),body:Center(child:Text("Setup biometric authentication")));}
