import "package:flutter/material.dart";class OtpVerificationPage extends StatelessWidget{final String? verificationId;final bool isPasswordReset;const OtpVerificationPage({super.key, this.verificationId, this.isPasswordReset=false});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text("OTP Verification")),body:Center(child:Text("Enter the code sent to your phone")));}
